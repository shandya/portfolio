export const metadata = {
  title: "Archive | Shandy Ardiansyah",
  description: "Archive for past projects",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
